services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: local_user
      POSTGRES_PASSWORD: local_password
      POSTGRES_DB: my_forever_dates
    ports:
      - '5432:5432'

  mailcatcher:
    image: sj26/mailcatcher
    ports:
      - '1025:1025'
      - '1080:1080'
